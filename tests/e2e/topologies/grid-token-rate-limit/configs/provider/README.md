# Provider Gateway

Place the shared provider-gateway Praxis configuration template here. It owns
the authenticated provider boundary, provider-route authorization, and
final-hop credential injection.
