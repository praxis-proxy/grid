apiVersion: forge.praxis.dev/v1alpha1
kind: Environment

metadata:
  name: minimal

spec:
  runtime:
    provider: auto
    clusterPrefix: forge

  network:
    crossCluster: false
    dnsZone: forge.test

  clusters: []

  services: []

  stacks: {}
