apiVersion: grid.praxis-proxy.io/v1alpha1
kind: GridSite
metadata:
  name: west-provider
  labels:
    grid.praxis-proxy.io/provider-site: west-provider
spec:
  gridNetworkRef: glb-demo
  region: west
  zone: west-provider-1
