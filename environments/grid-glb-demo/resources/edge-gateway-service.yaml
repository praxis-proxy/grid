apiVersion: v1
kind: Service
metadata:
  name: edge-gateway
  namespace: grid-system
  labels:
    app.kubernetes.io/name: edge-gateway
    app.kubernetes.io/part-of: grid-glb-demo
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: edge-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
